import { FilterProgressPipe } from './filter-progress.pipe';

describe('FilterProgressPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterProgressPipe();
    expect(pipe).toBeTruthy();
  });
});
